= simple_form_for(link) do |f|
  - if link.errors.any?
    #error_explanation
      %h2
        = pluralize(link.errors.count, "error")
        prohibited this link from being saved:
      %ul
        - link.errors.full_messages.each do |message|
          %li= message
          
  =f.input :endpoint_id, collection: endpoints_collection
  =f.input :crm_id, collection: crms_collection
  .actions
    = f.submit
